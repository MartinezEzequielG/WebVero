<style is:global>
	:root {
		color-scheme: light dark;
	}

	html {
		
		scroll-behavior: smooth;
	}
	
	body {
		background: linear-gradient(to bottom, #f4bcff, #bb99fa);;
		color: #fff;
		display: flex;
		margin: 0;
		flex-direction: column;
		min-height: 100vh;
	}
	
	@media (prefers-reduced-motion: reduce) {
		html {
			scroll-behavior: auto;
		}
	}
	
</style>